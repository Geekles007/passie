import React from 'react';
import Mockup from '../shared/mockup';
import Swiper from './swiper';

const SwipeToRedirect = () => {
  return (
    <Mockup>
      <Swiper />
    </Mockup>
  );
};

export default SwipeToRedirect;
